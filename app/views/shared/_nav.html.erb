<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Franbase', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li class="<%= 'active' if params[:controller] == 'docs' %>"><%= link_to 'Docs', docs_path %></li>
          <li class="<%= 'active' if params[:controller] == 'tags' %>"><%= link_to 'Tags', tags_path %></li>
          <li>
            <p class='navbar-btn'>
              <%= link_to "<i class='glyphicon glyphicon-plus'></i> New Doc".html_safe, new_doc_path, class: 'btn btn-info' %>
            </p>
          </li>      
          <li>
            <form class="navbar-form navbar-left" role="search" action="<%= docs_path %>">
              <div class="form-group">
                <input name="q" type="text" class="searchbox form-control" placeholder="Search Docs &amp; Tags">
              </div>
              <button type="submit" class="btn btn-default">Go</button>
            </form>
          </li>
        </ul>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>      
        <li><%= link_to 'Log In', new_user_session_path %></li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>