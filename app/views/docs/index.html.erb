<div>
  <h2><%= @page_title %></h2>
<ul>
  <% if @docs.count > 0 %>
    <% @docs.each do |doc| %>
      <li><%= link_to doc.name, doc %></li>
    <% end %>
  <% else %>
    There are no docs.
  <% end %>
</ul>
</div>